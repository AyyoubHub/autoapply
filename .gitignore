# Python virtual environment
.venv/
venv/
__pycache__/
*.pyc
*.pyo

# Real credentials — never commit these.
# Copy configs/config.example.json → configs/config.json and fill in.
configs/config.json

# API keys (.env.example is committed; .env is not)
.env

# Personal resume files compiled by the AI agent
tex/

# Runtime data that accumulates during use (not committed)
scratch/*.log

# Browser binary — install separately (see README for install_browser.py)
browser/
scripts/tectonic

# Logs (generated at runtime)
logs/

# Dev scratchpad
test_run.py

# Root-level debug / analysis text dumps
/*.txt
!/requirements.txt

# Runtime scratch data (personal application data, debug dumps)
scratch/*.json
scratch/*.log

# Coverage and Testing
.coverage
.coverage.*
htmlcov/
.pytest_cache/
pytest.xml

# OS and Editors
.DS_Store
.vscode/
.idea/

# SQLite databases — contain personal application history, never commit
autoapply.db
history.db
test_*.db
manual_test.db

# Applied jobs history (legacy JSON, superseded by SQLite)
**/*applied.json

# Ignore root .txt files but keep requirements.txt
# (rule already applied above — kept here as comment for clarity)